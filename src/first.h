#ifndef FIRST_H
#define FIRST_H
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
typedef enum {Home, Cell, School} place;
typedef struct t_nNode nNode;
typedef struct t_pNode pNode;
typedef struct t_phone phone;
typedef struct t_person person;


#endif
